apiVersion: v1
kind: Service
metadata:
  name: retail-store-ui-service
  labels:
    app: retail-store-ui
spec:
  selector:
    app: retail-store-ui
  ports:
    - port: 8080
      targetPort: 8080
  type: NodePort